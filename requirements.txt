# Runtime dependencies for email-security-pipeline
# Versions pinned to match CI green state as of 2026-09-13
# Update via: grep-ci logs + verify locally before bumping

requests==2.34.2
numpy==2.5.2
opencv-python-headless==4.13.0.92
python-dotenv==1.2.3
pyahocorasick==2.3.1
