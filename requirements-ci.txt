-r requirements.txt

# CI/Test dependencies
# NOTE: This intentionally avoids optional NLP/ML stacks (torch/transformers)
# NOTE: pytest is also in requirements-dev.txt; keep both lists in sync

pre-commit==4.6.2
pytest==9.1.1
